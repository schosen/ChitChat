<nav class="navbar navbar-light" style="background-color: #e98888b8;">

        <%= link_to root_path, :action => 'go' do %>
          <div>
          <img src="https://i.imgur.com/oWcqCmw.png" width="140" height="70" class="d-inline-block align-top" alt="">
          </div>
        <% end %>
      <span class="navbar-text">
        <% if current_user %>
          <ul class="nav-item dropdown">
          <% if current_user.avatar.attached? %>
              <% user_pic = image_tag current_user.avatar, size: 30, class: "avatar" %>
            <% else %>
              <% user_pic = image_tag "default-profile.png", size: 30, class: "avatar" %>
            <% end %>
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Logged in as <%= current_user.email %> <%=user_pic%>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <a class="dropdown-item" <%= link_to 'Profile page', user_path(current_user) %></a>
              <a class="dropdown-item" <%= link_to 'Edit Account', edit_user_registration_path %></a>
              <a class="dropdown-item" <%= link_to 'Logout', destroy_user_session_path, method: :delete %></a>
            </div>
          </ul>
        <% else %>
          <%# link_to "Log In", login_path %> or
          <%# link_to "Sign Up", signup_path %>
        <% end %>
      </span>
    </nav>
  <br>
<div class="center-text">
  <h1 class="typewriter">What would you like to chat about?</h1>
</div>

<br>
  <div class="new-chat-form">
    <img src="https://i.imgur.com/mDeCSid.png" width="300" height="150" >
    <%= render partial: 'form' %>
  <div>