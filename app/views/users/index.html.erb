<br><br>
<div class="container">
  <h2 align="center" class="header-text"> Make your profile stand out </h2>
  <br>

  <div align="center">
    <div class="c-top-container">
      <%= form_for(current_user, url: update_profile_path, method: 'post') do |f| %>
        <div class="c-input">
          <h4><%= f.label :bio %></h4>
            <br>
            <div class="small-field">
              <%= f.text_field :bio, class:"form-control" %>
            </div> 
        </div>
      <br>
          <div class="actions">
            <button class="btn btn-primary"<%= f.submit %>Save</button>
          </div>
      <br>
      <% end %>
    </div>
  </div>
</div>